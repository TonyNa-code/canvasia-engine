# Canvasia Engine Maintainer Guide

This guide explains the current maintenance boundaries for contributors who want to extend the engine without turning the main editor server into a harder-to-change file.

## Core Boundaries

- `run_editor.py` remains the source editor entry point. It owns the local HTTP server, project mutation commands, export commands, and packaging commands.
- `run_editor.py` also owns final writes for localization imports through `/api/import-localization-patches`; keep filesystem mutation for translated character / chapter / scene / block data there, while the frontend module prepares import plans.
- `editor_local_security.py` owns loopback-only editor API checks. Keep Host, Origin, and Referer policy changes there instead of spreading request-safety rules through handlers.
- `editor_snapshot_cache.py` owns small immutable-snapshot cache behavior. Reuse `SnapshotCache` for expensive read-mostly payloads that can be invalidated by a file signature.
- `openai_asset_generation.py` owns image-generation API calls and returned-file validation.
- `prototype_editor/modules/` owns browser-side editor logic that can be tested without a browser DOM.
- `prototype_editor/modules/story_block_catalog.js` owns the shared story-card registry: canonical labels, compact labels, Runtime capability declarations, timeline groups, effect-card groups, and reusable branch / music helper constants. When adding a new story block type, register it there first, then wire editors, import mapping, runtime playback, and any specialized audits from that shared metadata instead of creating another local block-type list.
- `prototype_editor/modules/music_range_scope.js` owns BGM coverage-range helpers for story cards: valid end-block candidates, effective after-block selection, range labels, live preview summaries, timeline chips, and range health diagnostics. Keep future music-scope UX, cue-span wording, or range validation helpers there so `app.js` only passes the selected scene and current block into pure helpers.
- `tests/test_frontend_story_block_registry_integration.py` guards the story-card registry contract: every registered card must have a default editor block factory, an add-card action, labels, and Runtime capability rows. If a new card fails this test, finish the missing wiring before merging.
- `prototype_editor/modules/command_palette.js` owns Cmd/Ctrl+K command metadata, filtering, recent-command helpers, and list rendering. Keep command definitions there, route execution through existing editor actions, and reuse `story_templates.js` for scene-aware story-template recommendations instead of creating another template-scoring list.
- `prototype_editor/modules/editor_mode.js` owns beginner / advanced mode labels, story-toolbar filtering, mode guide cards, and the beginner story workflow banner. Keep workflow wording and mode-specific next-step rendering there, but reuse `story_templates.js` for scene-aware template suggestions so the banner stays aligned with the story template panel, command palette, and production board.
- `prototype_editor/modules/beginner_tutorial.js` owns beginner-facing onboarding and the dashboard creator workflow state machine: tutorial steps, starter-kit panels, the six-stage project workflow, status labels, tone classes, and dashboard workflow rendering. Keep future no-code onboarding wording or "what should I do next" logic there; `app.js` should only gather current project state and pass it into the module.
- `prototype_editor/modules/beginner_assets_guide.js` owns beginner-facing asset-gap guidance: choosing the next material action, voice placeholder matching prompts, selected-missing-asset replacement prompts, urgent referenced gaps, ready-state routing, and the asset-guide panel markup. Keep asset inventory math in `asset_catalog.js`; `app.js` should only pass the current selected asset, type label, gap overview, and shared rendering helpers.
- `prototype_editor/modules/beginner_character_guide.js` owns beginner-facing character-page guidance: the character summary metrics card, empty-roster starter prompt, first-line prompt, missing-voice prompt, ready character prompt, and character-guide panel markup. Keep character dialogue/voice statistics in the existing character analysis helpers; `app.js` should only pass the selected character, stats, roster overview, and shared rendering helpers.
- `prototype_editor/modules/character_presentation_panel.js` owns character-presentation HTML rendering: presentation mode controls, fallback sprite / Live2D / 3D asset selectors, Live2D behavior toggles, presentation readiness cards, expression-level Live2D / 3D bindings, and save/import action markup. Keep presentation defaults, readiness scoring, asset-reference collection, and expression binding status helpers in their existing data modules / app helpers; `app.js` should only build the current presentation model and pass shared rendering helpers.
- `prototype_editor/modules/story_templates.js` owns reusable story template presets, block recipes, template summaries, variable requirements, BGM range recipe hints, scene-aware template recommendations, and the story-page template panel order. Add or tune scene recipes there, while `app.js` and `command_palette.js` should only translate recommendations into UI surfaces or executable editor actions.
- `prototype_editor/modules/story_template_application.js` owns turning story-template recipes into concrete story cards: cloning template fields, speaker placement, choice effects, variable conditions, default jump targets, BGM after-block ranges, and insertion summaries. Keep future template execution rules there; `app.js` should only ensure starter variables, pick the insertion point, persist the scene, and switch to the story screen.
- `prototype_editor/modules/story_template_panel.js` owns story-template panel rendering: recommended template items, button descriptions, insertion-preview chips, variable-requirement hints, and button markup. Keep future template-card UX here so `app.js` only passes the selected scene and shared render helpers.
- `prototype_editor/modules/script_importer.js` owns plain-text and lightweight VN script parsing for the story page. Keep line-format recognition, quoted dialogue, choice `[variable +1; flag=true]` consequences, standalone variable cues such as `set route = common` / `add affection +1`, `if variable >= value -> scene else -> scene`, `scene / show / hide / show stage scale / x / y / opacity / layer / flip / speed / play music / play sound / play video / wait / pause / shake / flash / zoom / pan / filter / blur / particle / credits / voice / jump` cue detection, Ren'Py-style menu target binding, voice-hint and text-speed attachment, choice grouping, preview summaries, and import limits there; `app.js` should only connect parsed draft blocks to project assets, project scenes, variables, the selected scene, and persistence flow.
- `prototype_editor/modules/script_importer_panel.js` owns the story-page text-to-card importer UI: beginner-readable supported-syntax cards, sample draft generation, preview summary wording, textarea placeholder, preview lines, and insert button state. Keep parser rules in `script_importer.js` and mapping rules in `script_import_mapping.js`; `app.js` should only pass current scene, selected insertion point, draft state, and shared helpers.
- `prototype_editor/modules/script_import_mapping.js` owns the pure lookup and draft-block normalization helpers used after script parsing: imported character hints, expression hints, asset hints, scene hints, effect-duration buckets, and the conversion from draft blocks into real story cards. Keep fuzzy matching and import-to-card rules there so `app.js` does not grow another parser-shaped branch.
- `prototype_editor/modules/typewriter.js` owns editor-preview typewriter reveal rules: Unicode grapheme stepping, leading / trailing quote grouping, Latin token grouping, punctuation pauses, inline decimal / domain period handling, and text-speed delays. Keep future browser-side text reveal effects there so `app.js` only schedules preview ticks, and keep it behaviorally aligned with `export_player_template/runtime_text_effects.js` through `tests/test_frontend_typewriter_parity.py`.
- `prototype_editor/modules/script_readability.js` owns browser-side text readability and script-quality analysis: long-text splitting, choice-text comfort checks, empty text / duplicate nearby text / missing punctuation / speaker-expression hints, script-quality Markdown / CSV shaping, and project-level digests. Keep future dialogue QA rules here, and let `scene_pacing_advisor.js` consume only the subset that directly affects playable scene rhythm.
- `prototype_editor/modules/story_scene_structure_panel.js` owns story-page scene-structure rendering: playable-gate status, structure metrics, highlight cards, next-step cards, and clickable scene-optimizer actions. Keep future "what should I do next in this scene" UX here; `app.js` should only build the current scene overview, render optional mood-recipe HTML, and pass shared helpers into the panel.
- `prototype_editor/modules/project_runtime_settings.js` owns browser-side project runtime setting defaults and sanitization. `project_settings.js`, `renpy_exporter.js`, and future runtime/export panels should call this module instead of duplicating save-slot, text-speed, audio-volume, voice-toggle, or voice-ducking ratio rules.
- `project_runtime_settings.py` owns project-level runtime defaults and sanitization for save-slot counts, default text speed, theme mode, audio volumes, voice toggles, and voice-ducking ratio. Reuse it from backend entry points and export builders instead of copying runtime-setting defaults into `run_editor.py`, `renpy_export.py`, or future packaging scripts.
- `prototype_editor/modules/dashboard_search_panel.js` owns dashboard global-search HTML rendering: mode chips, idle/search summaries, suggestion tags, grouped result sections, and scene / character / line result cards. Keep search scoring and normalization in `editor_filters.js`; `app.js` should only build the current overview, update DOM hosts, and pass shared rendering helpers.
- `prototype_editor/modules/renpy_exporter.js` owns editor-side Ren'Py draft conversion: labels, image / character / variable declarations, dialogue, narration, per-line text-speed tags, choices, variable effects, condition branches, voice cues, background transition timing, BGM / SFX volume and loop hints, scoped BGM stop / fadeout planning, timed / volume-adjusted video cutscenes, timed sprite slide / rise / pop transition mapping, screen flash / fade parameter mapping, basic camera zoom / pan / filter / blur mapping, SnowBlossom particle ambience / single-image texture mapping, credits, jumps, and migration-review warnings. Keep future `.rpy` syntax mapping and review-manifest wording there; `app.js` should only trigger export downloads and display status. When adding shared Ren'Py export rules, update `getRenpyExportContract()` so the JS/Python parity test can catch drift.
- `renpy_export.py` owns export-build-side Ren'Py Starter Bundle generation: script / options / screens / manifest / quality report / verifier output, asset references, generated dialogue textbox styling, optional textbox panel-image Frame backgrounds and bound font assets from copied UI resources, and the Python renderers for condition branches, timed / volume-adjusted video cutscenes, credits, variables, voice cues, background transition timing, BGM / SFX volume and loop hints, scoped BGM stop / fadeout planning, timed sprite slide / rise / pop transition mapping, per-line text-speed tags, screen flash / fade parameter mapping, basic camera zoom / pan / filter / blur mapping, and SnowBlossom particle ambience / single-image texture mapping. Keep it aligned with `prototype_editor/modules/renpy_exporter.js` whenever a story-card mapping changes so quick editor downloads and full export packages do not drift. When changing shared rule tables, update `get_renpy_export_contract()` and run `tests/test_renpy_export_contract.py`.
- `prototype_editor/modules/route_analyzer.js` owns pure scene-route analysis: route extraction, entry reachability, shortest entry paths, route QA case generation, broken-link detection, orphan / unreachable / ending metrics, route depth, validation issue counts, and per-scene production readiness. Keep future branch simulation or route scoring there so dashboard, project doctor, release gates, and tests share one route model.
- `prototype_editor/modules/project_doctor.js` owns beginner-facing repair-queue aggregation: validation issues, route alerts, preview-regression results, and stage-continuity audit rows are normalized into prioritized "what to fix next" steps with recovery / done-when wording. Keep source-specific checks in their own modules, then feed normalized rows into Project Doctor instead of duplicating route, stage, or regression analysis in `app.js`.
- `prototype_editor/modules/project_doctor_panel.js` owns Project Doctor panel HTML rendering: repair-queue step cards, summary metrics, safe-repair preview/confirm controls, receipt slot placement, and empty-state wording. Keep repair decisions in `project_doctor.js`; `app.js` should only pass the current queue builders, receipt renderer, and shared rendering helpers into this panel module.
- `prototype_editor/modules/project_history_panel.js` owns Project Safety Net panel HTML rendering: snapshot summary metrics, recovery notice markup, search and filter controls, and timeline placement. Keep snapshot sanitation/filtering in `project_history.js`; `app.js` should only pass the current history state, filtered snapshots, and focus-preserving rerender flow into this panel module.
- `prototype_editor/modules/project_milestones.js` owns beginner-friendly milestone planning and scoring: first-playable Demo, experience preview, release candidate, final public release, gap digests, next-action briefs, check rendering helpers, production-backlog signals, and progress percent shaping. Keep future "what should I do next" decision rules here; `app.js` should only pass current project statistics and route analysis into this module.
- `prototype_editor/modules/project_milestones_panel.js` owns project-milestone panel HTML rendering: the dashboard action brief, full milestone route, compact inspection/preview milestone card, milestone gap digest, and card-level markup. Keep the visual layout and small presentational fallbacks here; `project_milestones.js` should decide readiness and next actions, while `app.js` only passes current project statistics plus rendering helpers.
- `prototype_editor/modules/route_testing_report.js` owns route QA report serialization, pre-release playtest workbook lanes, manual route steps, variable / status hints, Markdown tables, standalone Markdown export content, and CSV rows. Keep route report and workbook formatting there instead of adding more report-specific table logic to `app.js`.
- `prototype_editor/modules/scene_production_board.js` owns scene-level production task analysis and export formatting: per-scene completion scoring, next-action suggestions, recommended scene recipes, missing background / BGM / voice checks, text-length checks, choice crowding checks, script-quality rollups from `script_readability.js`, bad route targets, Markdown, and CSV. Keep production-first diagnostics there, but reuse `story_templates.js` for scene-aware recipe scoring so the story page, command palette, and production board do not drift into separate recommendation systems.
- `prototype_editor/modules/project_polish.js` owns the release-safe one-click polish plan: readable text splitting, scene presentation polish, BGM range / fade fixes, project-level save-slot defaults, textbox readability patches, UI skin/font binding defaults, scene pacing snapshots, VN essentials / Runtime readiness snapshots, receipt data, Markdown, and clipboard summaries. Keep future "safe automatic fix" planning here, reuse specialist audit modules for diagnostic-only snapshots, and let `app.js` only create the safety checkpoint, persist scene plans, save project patches, reload, and show the receipt.
- `prototype_editor/modules/voice_match_review_panel.js` owns browser-side voice-file match review rendering: ambiguous/unmatched buckets, manual target pickers, candidate tags, disabled no-target states, and review dismissal controls. Keep filename matching, candidate selection, and review-session construction in `script_voice.js`; `app.js` should only pass the current review session, available voice targets, and shared rendering helpers.
- `prototype_editor/modules/voice_production_sheet.js` owns project-level voice production analysis and export formatting: per-line voice readiness, missing voice bindings, missing or wrong-type voice assets, missing files, unknown speakers, long-line checks, per-speaker progress, Markdown, and CSV. Keep future voice handoff / recording QA logic there instead of growing `app.js` or the story-card editors.
- `prototype_editor/modules/screenplay_exporter.js` owns full screenplay / production-script exports: chapter and scene ordering, dialogue / narration / choice / stage-direction flattening, voice-binding notes, status digest, Markdown, and CSV. Keep future proofreading, voice-actor handoff, translator script, or external-writing export logic there; `app.js` should only expose buttons, panel summaries, and downloads.
- `prototype_editor/modules/director_cue_sheet.js` owns director-facing scene cue sheets: per-scene story / visual / audio / route / effect beats, required assets, scene readiness, production queue items, Markdown, and CSV. Keep future storyboard, shot-list, or episode-director handoff logic there, and feed only its normalized production queue into `production_backlog.js`.
- `prototype_editor/modules/preview_regression.js` owns automatic preview-regression seed selection, branch-choice targeting, and best-effort condition / fallback variable presets for route smoke tests. Keep future route-smoke prioritization there so `app.js` only runs the selected seeds through the editor preview runtime.
- `prototype_editor/modules/playtest_handoff_report.js` owns tester-facing playtest handoff exports and intake: route cases, regression smoke results, fix queues, Markdown work orders, feedback templates, feedback CSV parsing, intake summaries, and CSV rows. Keep tester handoff wording and table formats there so `app.js` only wires buttons, downloads, and local file selection.
- `prototype_editor/modules/choice_consequence_sheet.js` owns choice consequence analysis and export formatting: option target summaries, variable-effect summaries, fake-choice warnings, duplicate option checks, broken target checks, broken variable checks, Markdown, and CSV. Keep future choice-design QA there instead of scattering branch-consequence rules across route and inspection UI code.
- `prototype_editor/modules/variable_influence_sheet.js` owns variable influence analysis and export formatting: variable definitions, read/write locations, choice effects, condition reads, unknown references, type mismatch checks, range checks, Markdown, and CSV. Keep future flag / affection / route-variable QA there instead of scattering variable-audit logic across story editing and inspection UI code.
- `prototype_editor/modules/asset_dependency_sheet.js` owns asset dependency analysis and export formatting: story-card asset references, character sprite / Live2D / 3D bindings, project UI skin bindings, missing-file checks, unknown references, type mismatch checks, unused asset hints, Markdown, and CSV. Keep future asset-impact QA there instead of scattering dependency rules across asset management, character editing, project settings, and inspection UI code.
- `prototype_editor/modules/asset_footprint.js` owns asset package-size analysis and export formatting: per-asset byte normalization, package-size risk levels, category totals for images / audio / video / Live2D / 3D / fonts, unknown-size warnings, unused large-asset hints, Markdown, and CSV. Keep future compression budgeting, bundle-size scoring, and release footprint wording there instead of growing `app.js` or duplicating package-size rules in export screens.
- `prototype_editor/modules/runtime_preload_budget.js` owns editor-side Runtime startup-pressure analysis and export formatting: entry-scene / early-route preload candidates, first-screen byte budgets, missing first-screen files, single-asset preload risks, scene hotspots, Markdown, and CSV. Keep future startup-loading advice here so `app.js` remains a thin inspection card and export button shell, and keep the phase rules aligned with `export_runtime_preload.py`. Web-playback execution policy lives in `export_player_template/runtime_preload.js`: performance profiles should stage `critical`, `early`, `deferred`, and `library` work through idle batches instead of reintroducing eager all-at-once preload behavior; cached-asset reuse, skipped asset status, loaded / failed asset IDs, cache-efficiency percentage shaping, player HUD status text, preload size formatting, and preload controller progress snapshots also belong there rather than in `player.js`. Native preload status accounting lives in `native_runtime/runtime_preload.py`; keep cached-entry reuse, ready-entry counts, loaded / cached asset ID lists, size accounting, and status-line wording aligned with the Web Runtime so Desktop exports do not drift. Route-aware playback prefetch planning lives in `export_player_template/runtime_scene_prefetch.js`; keep scene / choice / jump asset discovery there so `player.js` only starts or stops the lightweight prefetch controller. Web Runtime text reveal rules live in `export_player_template/runtime_text_effects.js`; keep Unicode grapheme stepping, punctuation pauses, and text-speed delay math there so `player.js` only schedules reveal ticks. Web Runtime language normalization, localized text fallback, fallback metadata, and fallback-report aggregation live in `export_player_template/runtime_i18n.js`; keep language-code canonicalization, supported-language fallback, language labels, translation fallback order, missing-translation result metadata, and report summaries there so `player.js` only records current playback diagnostics. Native Runtime language normalization, localized text fallback, fallback metadata, and fallback-report aggregation live in `native_runtime/runtime_i18n.py`; keep it behaviorally aligned with the Web module so exported Web/Desktop and native packages do not drift. Native route-aware prefetch planning lives in `native_runtime/runtime_scene_prefetch.py`; keep it pure and testable, and let `native_runtime/runtime_player.py` only provide the current scene snapshot and warm returned entries. Native runtime diagnostics live in `native_runtime/runtime_diagnostics.py`; keep preload / prefetch / cache summary shaping, cache-efficiency percentages, export-level diagnostics JSON / Markdown, and static entry-scene diagnosis there so the player overlay and release reports do not drift.
- `prototype_editor/modules/asset_rights_sheet.js` owns release-facing asset rights and credits analysis plus the asset-detail rights form: license labels, source / author records, commercial-use status, attribution needs, placeholder markers, AI provenance hints, Staff / Credits rows, Staff / Credits draft generation, Markdown, CSV, form-field collection, and the inspection-page card. Keep future publishing-rights QA there; `asset_dependency_sheet.js` should remain focused on whether assets are referenced and safe to delete.
- `prototype_editor/modules/audio_cue_sheet.js` owns BGM cue-sheet analysis and export formatting: music range summaries, missing BGM asset checks, invalid range targets, takeover warnings, fade suggestions, voice / BGM mix-risk checks, Markdown, and CSV. Keep future audio scheduling or mix-readability checks there so `app.js` remains a thin inspection/export surface.
- `prototype_editor/modules/stage_direction_sheet.js` owns character stage-direction analysis and export formatting: background coverage, character show / hide continuity, speaker auto-placement warnings, expression and visual asset readiness, stage composition overlap / crowding / layer / low-opacity speaker checks, scene continuity audit rollups, Markdown, and CSV. Keep future staging continuity checks there instead of adding more production-audit logic to `app.js`.
- `prototype_editor/modules/presentation_timeline.js` owns release-facing presentation timeline analysis and export formatting: scene rhythm, text duration estimates, visual / audio anchors, hard-cut warnings, long static text runs, Markdown, and CSV. Keep future production-timeline rules there so inspection UI remains a thin shell.
- `prototype_editor/modules/localization_coverage.js` owns editor-side i18n coverage and translator handoff logic: supported-language discovery, chapter / scene / character / dialogue / narration / choice text coverage, missing translation checks, same-as-source warnings, Markdown / CSV export, CSV parsing, and safe import-plan generation. Keep future translation QA there so runtime fallback logic and inspection UI stay separate.
- `prototype_editor/modules/runtime_capability_matrix.js` owns editor-to-runtime coverage reporting, VN essentials maturity auditing, and export acceptance guidance: project block usage, Web Runtime coverage, native Runtime coverage, partial / unknown support detection, core visual-novel readiness checks such as BGM range / fade coverage, textbox readability, save-slot count, UI skin readiness, generated Runtime acceptance checklist items, Markdown, and CSV. Keep block support declarations sourced from `story_block_catalog.js`, and put only matrix-specific QA wording, essentials scoring, or acceptance checklist logic here.
- `prototype_editor/modules/unlockable_content_manifest.js` owns EXTRA / replay / archive readiness reporting: CG gallery, music room, voice replay, character archive, location archive, narration archive, relationship archive, chapter replay, ending collection, generated achievement coverage, missing unlockable assets, and Markdown / CSV exports. Keep future gallery or achievement release checks there; `app.js` should only pass current project data and route analysis into it.
- `prototype_editor/modules/production_backlog.js` owns cross-module production task aggregation and export formatting: route, scene, director cue, voice, choice, variable, asset dependency, asset rights, audio, stage issues, stage-continuity audit rows, presentation, localization, runtime coverage, Runtime preload budget, and unlockable-content issues are normalized into one prioritized work queue with Markdown and CSV exports. It must accept the standard route testing plan shape from `route_analyzer.js` and derive route execution blockers itself when no prebuilt execution queue is present. Keep future task scoring / ownership rules there instead of duplicating queue logic across inspection cards.
- `prototype_editor/modules/release_control.js` owns editor-side final publish gates, ordered release-fix plans, and release-facing summaries: validation blockers, route reachability, route-playtest blockers derived from the standard route testing plan, asset gaps, creative-quality polish, Runtime preload budget pressure, production-backlog next-task details, voice warnings, version / resolution checks, unused assets, desktop export confirmation, release-report overview rows, release-fix table rows, next-action cards, and next-step advice are normalized into one publish queue. Keep future release decision wording, prioritization, Runtime startup-risk report shapes, overview metrics, next-action advice, route-playtest fix step shaping, production-backlog task serialization, report-table row shaping, next-action-card shaping, and first-action serialization here; `app.js` should only pass current project context into these pure helpers and render / export any `routeIssueQueue`, `productionBacklogTask`, or next-action-card details without recomputing route priority.
- `prototype_editor/modules/release_control_panel.js` owns release-control panel HTML rendering: next-action card markup, release-fix step cards, final publish gate markup, release-checklist cards, route issue previews, production-backlog detail blocks, and the release-control panel button cluster. Keep release-panel layout and small presentational fallbacks there; `release_control.js` should keep deciding what the next action is, and `app.js` should only pass builder functions plus rendering helpers into this panel module.
- `prototype_editor/modules/release_candidate_manifest.js` owns public-preview handoff manifests: project inventory, release deliverable status, unlockable-content readiness rollup, risk register normalization, manual signoff checks, Markdown / CSV exports, and the inspection-page manifest card. Keep future tester / publisher handoff wording there; `app.js` should only assemble the current context and call the module.
- `native_runtime/runtime_player.py` owns native playback, native release checks, and package-local Runtime reports, including the one-command doctor JSON / Markdown outputs and crash-feedback CLI writers. Keep report builders and CLI report writers there when they need to exercise the exported bundle itself; `run_editor.py` should call those commands, provide fallback files when generation fails, and index the generated reports in manifests / evidence packs. Crash-log storage and privacy-conscious crash-summary shaping live in `native_runtime/runtime_storage.py`; keep raw log writes, home-path redaction, and recent-log enumeration there.
- `export_package_guide.py` owns export-side `README_试玩验收先看这里.md` generation for Web, desktop, and native runtime packages: launch steps, tester acceptance focus, important package files, missing-asset summary, and platform notes. Keep guide wording there instead of adding more package-readme prose to `run_editor.py`; the export functions should only pass target-specific launch steps and report names.
- `export_localization_audit.py` owns export-side `localization_audit.json` and `localization_audit.md` generation: supported-language normalization, missing translation detection, coverage percentages, and translator-facing report wording. Keep multilingual coverage rules there instead of mixing translation checks into `run_editor.py` or release scoring.
- `export_quality_reports.py` owns the shared export-quality report bundle for Web, desktop, and native packages: story route map, route playtest workbook, localization audit, release-readiness summary, report-file ordering, and report-file de-duplication. Keep future cross-export quality-report orchestration there instead of adding the same write sequence to each export function in `run_editor.py`.
- `export_release_control.py` owns editor-side native Runtime release-control fallback logic: quality-gate status, next-step ordering, machine-readable payloads, and concise Markdown rendering. Keep future release-gate aggregation there; `run_editor.py` should only provide project-specific report names and write the generated files.
- `export_release_readiness.py` owns export-side `release_readiness_summary.json` and `release_readiness_summary.md` generation: release gate labels, score calculation, missing-asset blockers, unlockable-content warnings, runtime warnings, and tester handoff next steps. Keep scoring and release-gate wording there instead of scattering package-readiness checks through `run_editor.py`; export functions should only pass manifest, missing assets, report names, and platform notes.
- `export_story_route_map.py` owns export-side `story_route_map.json` and `story_route_map.md` generation: scene graph extraction, implicit same-chapter continuation, choice / condition / jump targets, broken-route detection, entry reachability, unreachable-scene lists, and ending candidates. Keep route graph semantics there instead of duplicating story-flow analysis in `run_editor.py`; release-readiness generation may consume the route map summary but should not rebuild the graph.
- `export_route_playtest_workbook.py` owns export-side `route-playtest-workbook.json`, `route-playtest-workbook.md`, and `route-playtest-workbook.csv` generation: it consumes `export_story_route_map.py`, converts branches and endings into blocker-first manual QA lanes, adds variable / status hints, and writes tester-readable Markdown plus spreadsheet-friendly rows. Keep route-playtest wording and execution-priority shaping here instead of duplicating route QA tables in `run_editor.py`; when the schema changes, update `tests/test_export_route_playtest_workbook.py`, export smoke assertions, and `tools/ci/local_verify.py` together.
- `export_unlockable_manifest.py` owns export-side `unlockable_content_manifest.json` and `unlockable_content_report.md` generation: pure project / asset analysis, replay / gallery / archive / achievement groups, ending reachability, summary metrics, machine-readable JSON, and tester-readable Markdown. Keep file names centralized in `UNLOCKABLE_CONTENT_MANIFEST_FILE_NAME` / `UNLOCKABLE_CONTENT_REPORT_FILE_NAME`; `run_editor.py` should only call the module while assembling Web, desktop, and native packages. Update Web, desktop, native smoke assertions, focused module tests, and `tools/ci/local_verify.py` together whenever the schema or module surface changes. The frontend `prototype_editor/modules/unlockable_content_manifest.js` remains responsible for editor-side inspection wording and editor-triggered Markdown / CSV exports.

## Safe Extension Pattern

When adding a new backend capability:

1. Put reusable pure logic in a focused module when it can stand alone.
2. Keep `run_editor.py` as the command/API integration layer.
3. Add a small unit test for the pure module.
4. Add an integration or smoke test when the feature touches project files, exports, or runtime behavior.
5. Add new Python modules to `tools/ci/local_verify.py` so `--profile syntax` catches syntax errors before release.
6. If the feature changes shared Ren'Py export behavior, update both contracts and keep `Ren'Py export contract` in `tools/ci/local_verify.py --profile quick`.

When adding a new frontend-only editor capability:

1. Put data shaping, analysis, export formatting, and status-digest logic in `prototype_editor/modules/<feature>.js`.
2. Load the module before `./app.js` in `prototype_editor/index.html`, then update `tests/test_frontend_entrypoint_modules.py`.
3. Keep `prototype_editor/app.js` as a thin UI/download/action shell; avoid adding large parsers, report builders, or cross-module scoring there.
4. Add a module test such as `tests/test_frontend_<feature>_module.py`.
5. Add action-handler coverage in `tests/test_frontend_action_handlers.py` when the feature exposes buttons or command actions.

## Project Data Cache

The editor frequently asks `/api/project-data` for the full current project bundle. The bundle cache is intentionally conservative:

- It watches project metadata, assets, characters, variables, chapters, history, session state, and referenced asset files.
- It returns deep copies so UI handlers and tests cannot mutate the stored cache by accident.
- It refreshes automatically when watched file size or modified time changes.

If a future feature writes project files outside the existing helpers, make sure the watched signature includes the new file or call `clear_project_bundle_cache()` after the write.

## API Safety

The source editor is meant to run on loopback addresses such as `127.0.0.1` and `localhost`. API handlers should continue to reject non-local Host, Origin, or Referer values so another website cannot drive local project mutations.

When adding a new `/api/*` route:

- Keep it behind the shared `EditorRequestHandler` request guard.
- Return JSON errors for user-facing failures.
- Avoid writing outside the active project or approved export/cache directories.

## Recommended Checks

Use these checks before larger changes:

```bash
python3 tools/ci/local_verify.py --profile syntax
python3 -m unittest tests.test_editor_infrastructure tests.test_local_verify_tool -v
python3 -m unittest tests.test_run_editor_smoke -v
python3 -m unittest tests.test_release_public_surface -v
```

For frontend-only changes, also run the relevant `tests/test_frontend_*` module and `node --check` on the touched script.
